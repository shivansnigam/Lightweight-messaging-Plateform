<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit ur Chat</title>
</head>
<body>
    <p>Edit your chat <%= chat.id %></p>
    <p><b>This chat was sent from <%= chat.from %> to <%= chat.to %></b></p>

    <form method="post" action="/chats/<%= chat.id %>?_method=PUT">
        <textarea name="msg" rows="5" cols="10"><%= chat.msg %></textarea>
        <button>Edit</button>
    </form>

</body>
</html>
